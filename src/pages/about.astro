---
/**
 * about.astro
 * About Ignova page with team info and Naive.uno partnership
 */
import Layout from "../layouts/Layout.astro";
import NaiveSection from "../components/NaiveSection.astro";
import FinalCTA from "../components/sections/FinalCTA.astro";

const principles = [
  {
    icon: "💡",
    title: "Innovación Constante",
    description:
      "Nos mantenemos al día con las últimas tecnologías para ofrecerte las mejores soluciones.",
  },
  {
    icon: "🎯",
    title: "Enfoque en Resultados",
    description:
      "No solo escribimos código. Creamos software que genera valor real para tu negocio.",
  },
  {
    icon: "🤝",
    title: "Colaboración Transparente",
    description:
      "Comunicación clara sin jerga técnica. Eres parte del proceso de principio a fin.",
  },
];

const processSteps = [
  {
    number: "01",
    title: "Conversación",
    description: "Escuchamos tu idea y entendemos tus metas. Sin compromiso.",
  },
  {
    number: "02",
    title: "Propuesta",
    description:
      "Te presentamos un plan claro con alcance y presupuesto definidos.",
  },
  {
    number: "03",
    title: "Desarrollo",
    description: "Construimos tu solución con revisiones periódicas.",
  },
  {
    number: "04",
    title: "Lanzamiento",
    description: "Ponemos tu producto en línea y te acompañamos después.",
  },
];
---

<Layout title="Nosotros | Ignova">
  <!-- Hero Section -->
  <section class="py-20 md:py-28 bg-ignova-dark relative overflow-hidden">
    <!-- Gradient accent -->
    <div
      class="absolute top-0 right-0 w-96 h-96 bg-primary/10 rounded-full blur-[100px]"
    >
    </div>
    <div
      class="absolute bottom-0 left-0 w-96 h-96 bg-secondary/10 rounded-full blur-[100px]"
    >
    </div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-3xl mx-auto text-center">
        <span
          class="inline-block px-4 py-1 bg-primary/10 rounded-full text-primary font-semibold text-sm uppercase tracking-wider mb-6"
        >
          Sobre Nosotros
        </span>
        <h1
          class="text-4xl md:text-6xl font-hoves font-bold text-ignova-light mb-6"
        >
          Somos <span
            class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent"
            >Ignova</span
          >
        </h1>
        <p class="text-xl text-ignova-light/70 font-inter leading-relaxed">
          Estudio de desarrollo de software con visión global y ejecución
          precisa. Combinamos el empuje de un equipo joven con la rigurosidad
          técnica necesaria para crear productos digitales que funcionan. No
          solo escribimos código; construimos soluciones con propósito y sentido
          común.
        </p>
      </div>
    </div>
  </section>

  <!-- Principles Section -->
  <section class="py-20 md:py-28 bg-black/30">
    <div class="container mx-auto px-4">
      <div class="text-center max-w-3xl mx-auto mb-12">
        <h2 class="text-3xl md:text-4xl font-hoves font-bold text-ignova-light">
          Nuestros Principios
        </h2>
        <p class="mt-4 text-lg text-ignova-light/60 font-inter">
          Los pilares que guían cada proyecto que emprendemos.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {
          principles.map((p, index) => (
            <div
              class="bg-ignova-dark p-8 rounded-2xl border border-ignova-light/10 hover:border-primary/30 transition-all duration-300 animate-fade-in-up"
              style={`--anim-delay: ${index * 0.1}s;`}
            >
              <div class="text-4xl mb-4">{p.icon}</div>
              <h3 class="text-xl font-hoves font-bold mb-3 text-ignova-light">
                {p.title}
              </h3>
              <p class="text-ignova-light/60 font-inter">{p.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Process Section -->
  <section class="py-20 md:py-28 bg-ignova-dark">
    <div class="container mx-auto px-4">
      <div class="text-center max-w-3xl mx-auto mb-12">
        <h2 class="text-3xl md:text-4xl font-hoves font-bold text-ignova-light">
          Nuestro Proceso
        </h2>
        <p class="mt-4 text-lg text-ignova-light/60 font-inter">
          Un camino claro y sin sorpresas para llevar tu proyecto al éxito.
        </p>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
        {
          processSteps.map((step, index) => (
            <div
              class="relative animate-fade-in-up"
              style={`--anim-delay: ${index * 0.1}s;`}
            >
              <div class="h-1 w-full bg-gradient-to-r from-primary to-secondary rounded-full mb-6" />
              <p class="text-sm font-bold text-primary mb-2 font-hoves">
                {step.number}
              </p>
              <h3 class="text-xl font-hoves font-bold mb-2 text-ignova-light">
                {step.title}
              </h3>
              <p class="text-ignova-light/60 font-inter text-sm">
                {step.description}
              </p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Naive.uno Partnership -->
  <NaiveSection />

  <!-- CTA -->
  <FinalCTA />
</Layout>

<style>
  .animate-fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
    animation-delay: var(--anim-delay, 0s);
    opacity: 0;
  }
</style>
