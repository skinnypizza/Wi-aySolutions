---
/**
 * ProblemSection.astro
 * Section highlighting pain points Ignova solves
 * Dark theme with gradient accents
 */

const problems = [
    {
        icon: "⚡",
        title: "Desarrollo de alta calidad",
        description:
            "No nos conformamos con lo básico. Cada línea de código está diseñada para escalar y durar.",
    },
    {
        icon: "🤝",
        title: "Comunicación directa",
        description:
            "Sin intermediarios ni jerga técnica. Hablamos tu idioma y estamos a un WhatsApp de distancia.",
    },
    {
        icon: "🌎",
        title: "Visión Global",
        description:
            "Talento emergente redefiniendo los límites del mercado. Entendemos de dónde vienes, pero sabemos exactamente hacia dónde va el mundo.",
    },
];
---

<section class="py-20 md:py-28 bg-ignova-dark relative overflow-hidden">
    <!-- Subtle gradient accent -->
    <div
        class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-primary to-secondary"
    >
    </div>

    <div class="container mx-auto px-4">
        <!-- Header -->
        <div class="max-w-3xl mx-auto text-center mb-16">
            <h2
                class="text-3xl md:text-5xl font-hoves font-bold text-ignova-light mb-6"
            >
                ¿Por qué elegir a <span
                    class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent"
                    >Ignova</span
                >?
            </h2>
            <p class="text-lg text-ignova-light/70 font-inter">
                Somos el partner que tu proyecto necesita. Combinamos
                experiencia técnica con visión de negocio para entregar
                resultados reales.
            </p>
        </div>

        <!-- Features Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {
                problems.map((problem, index) => (
                    <div
                        class="relative p-8 rounded-2xl bg-black/30 border border-ignova-light/10 hover:border-primary/30 transition-all duration-300 animate-fade-in-up"
                        style={`--anim-delay: ${index * 0.15}s;`}
                    >
                        <div class="w-14 h-14 rounded-xl bg-gradient-to-br from-primary/20 to-secondary/20 flex items-center justify-center mb-6 text-3xl">
                            {problem.icon}
                        </div>
                        <h3 class="text-xl font-hoves font-bold mb-3 text-ignova-light">
                            {problem.title}
                        </h3>
                        <p class="text-ignova-light/60 font-inter leading-relaxed">
                            {problem.description}
                        </p>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    .animate-fade-in-up {
        animation: fade-in-up 0.6s ease-out forwards;
        animation-delay: var(--anim-delay, 0s);
        opacity: 0;
    }
</style>
