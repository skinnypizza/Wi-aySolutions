---
import { Image } from 'astro:assets';
import heroImage from '../../assets/hero-placeholder.jpg';
import Button from '../Button.astro';
---

<section class="py-20 md:py-32 overflow-hidden">
	<div class="container mx-auto px-4">
		<div class="grid grid-cols-1 items-center gap-12 lg:grid-cols-2">
			<div class="flex flex-col gap-6 text-center lg:text-left">
				<div class="flex flex-col gap-4 animate-fade-in-up" style="--anim-delay: 0.1s;">
					<h1
						class="text-4xl font-semibold leading-tight tracking-tight md:text-6xl font-display text-text-light dark:text-text-dark"
					>
						Tecnología que crece contigo
					</h1>
					<p class="text-lg font-normal leading-relaxed text-text-light/80 dark:text-text-dark/80 font-body">
						Fusionamos nuestras raíces ancestrales con tecnología de punta para crear soluciones que
						evolucionan con tu negocio.
					</p>
				</div>
				<div class="flex justify-center lg:justify-start animate-fade-in-up" style="--anim-delay: 0.2s;">
					<Button href="/services" size="lg">Conoce nuestras soluciones</Button>
				</div>
			</div>

			<div class="relative flex items-center justify-center animate-fade-in-up" style="--anim-delay: 0.3s;">
				<div class="absolute inset-0 bg-primary-DEFAULT/10 rounded-full blur-3xl z-0"></div>
				<Image
					class="relative w-full max-w-md rounded-xl shadow-lg z-10"
					alt="Imagen abstracta de tecnología y crecimiento"
					src={heroImage}
					width={600}
					height={600}
					format="webp"
					quality={80}
				/>
			</div>
		</div>
	</div>
</section>

<style>
	.animate-fade-in-up {
		animation: fade-in-up 0.6s ease-out forwards;
		animation-delay: var(--anim-delay, 0s);
		opacity: 0;
	}
</style>