---
import Button from '../Button.astro';
import Icon from '../Icon.astro';
---

<section class="relative bg-background-light dark:bg-background-dark py-24 md:py-32">
  <div class="container mx-auto px-4 text-center">

    <div
      class="max-w-3xl mx-auto flex flex-col gap-6 animate-fade-in-up"
      style="--anim-delay: 0.1s;"
    >
      <h1 class="text-4xl md:text-6xl font-display font-bold text-text-light dark:text-text-dark leading-tight">
        Tu negocio merece una web que <span class="text-primary">genere confianza y ventas</span>.
      </h1>

      <p class="text-lg md:text-xl font-body text-text-light/80 dark:text-text-dark/80">
        Deja de perder clientes con una web genérica. Creamos la presencia digital a medida que tu negocio en Bolivia y LATAM necesita para crecer. Clara, profesional y directa.
      </p>
    </div>

    <div
      class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-in-up"
      style="--anim-delay: 0.3s;"
    >
      <Button href="/contact" size="lg" variant="primary">
        Solicita tu Cotización
      </Button>
      <Button href="https://wa.me/591XXXXXXXX" size="lg" variant="outline">
        <Icon name="whatsapp" class="mr-2" />
        Hablemos por WhatsApp
      </Button>
    </div>

  </div>
</section>

<style>
	.animate-fade-in-up {
		animation: fade-in-up 0.6s ease-out forwards;
		animation-delay: var(--anim-delay, 0s);
		opacity: 0;
	}
</style>